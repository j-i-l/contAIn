# systemd unit — one-shot container image commit
# TEMPLATE: setup.sh renders @@INSTALL_DIR@@ then installs to /etc/systemd/system/

[Unit]
Description=contain Container State Commit

[Service]
Type=oneshot
ExecStart=@@INSTALL_DIR@@/contain-commit.sh
